[postgres]
host = #host name of the database server (default: localhost)
port = #port number of the database server (default: 5432)
user = #user name of the database server (default: postgres) but we use "mh"
pass = #password of the database server (default: postgres) but we use "mh"
db_name = #name of the database.
